#!/usr/bin/python
import sys

last_length = 0
for line in sys.stdin:
	line = line.rstrip()
	if len(line) >= last_length + 3:
		print("\n%s" % line)
	elif len(line) <= last_length - 3:
		print("\n%s" % line)
	else:
		print("%s" % line)
	last_length = len(line)
